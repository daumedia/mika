# Nur in Produktion geladen (APP_ENV=prod).
# Content-Security-Policy erzwingen (Report-Only-Durchlauf war sauber).
# Not-aus: in Coolify CSP_ENFORCE=0 setzen (echte Env-Variable sticht diese Datei).
CSP_ENFORCE=1
